{
  "name": "topstar_eng",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync": "node src/main.js",
    "serve": "node src/server.js",
    "start": "npm run sync && npm run serve",
    "install-browser": "npx playwright install chromium",
    "login": "node src/login.js",
    "refresh-session": "node src/refreshStorageState.js",
    "validate": "node src/validateHomeworkData.js",
    "build": "npm run sync && npm run validate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "cheerio": "^1.2.0",
    "dotenv": "^17.3.1",
    "playwright": "^1.58.2"
  }
}
